
// ejercicio 2


function calcular(){
    n1 = Number(prompt ("pone un numer --> "));
    n2 = Number(prompt("pone un numer --> "));
    n3 = Number(prompt("pone un numer --> "));

    resta = n1 - n2 - n3;
    multi = n1 / n2 / n3;
    if (resta > 30 && div < 400){
        console.log("Se cumplen");
    }else {
        console.log("No se cumple");
    }
}


calcular();


//ejercicio3
/*
function entreNumeros (){
    n1 = Number(prompt ("Pone un numer --> "));
    n2 = Number(prompt("pone un numer --> "));

    for ( let index = n1; index <= n2; index++){
        if ( index & 2 != 0) {
            console.log(index);
        }
    }
}

// entreNumeros();
*/